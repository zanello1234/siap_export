<?xml version="1.0" encoding="utf-8"?><?xml version="1.0" encoding="utf-8"?>

<odoo><odoo>

    <!-- Main Wizard View -->    <record id="view_siap_export_wizard" model="ir.ui.view">

    <record id="view_siap_export_wizard" model="ir.ui.view">        <field name="name">siap.export.wizard.form</field>

        <field name="name">siap.export.wizard.form</field>        <field name="model">siap.export.wizard</field>

        <field name="model">siap.export.wizard</field>        <field name="arch" type="xml">

        <field name="arch" type="xml">            <form string="SIAP Export">

            <form string="SIAP Export">                <div class="alert alert-info" role="alert">

                <div class="alert alert-info" role="alert">                    <p><strong>SIAP Export Tool</strong></p>

                    <p><strong>SIAP Export Tool</strong></p>                    <p>This tool exports the top 20 customers or suppliers to SIAP format for government compliance.</p>

                    <p>This tool exports the top 20 customers or suppliers to SIAP format for government compliance.</p>                </div>

                </div>                <group>

                <group>                    <group>

                    <group>                        <field name="export_type" required="1" widget="radio"/>

                        <field name="export_type" required="1" widget="radio"/>                        <field name="export_date" required="1"/>

                        <field name="export_date" required="1"/>                    </group>

                    </group>                    <group>

                    <group>                        <field name="format_type" required="1"/>

                        <field name="format_type" required="1"/>                        <field name="company_id" options="{'no_create': True, 'no_open': True}"/>

                        <field name="company_id" options="{'no_create': True, 'no_open': True}"/>                    </group>

                    </group>                </group>

                </group>                <footer>

                <footer>                    <button string="Preview Data" type="object" name="action_preview" 

                    <button string="Preview Data" type="object" name="action_preview"                             class="btn-primary" data-hotkey="p"/>

                            class="btn-primary" data-hotkey="p"/>                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="x"/>

                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="x"/>                </footer>

                </footer>            </form>

            </form>        </field>

        </field>    </record>

    </record>

    <record id="view_siap_export_preview_form" model="ir.ui.view">

    <!-- Preview Form View -->        <field name="name">siap.export.preview.form</field>

    <record id="view_siap_export_preview_form" model="ir.ui.view">        <field name="model">siap.export.wizard</field>

        <field name="name">siap.export.preview.form</field>        <field name="arch" type="xml">

        <field name="model">siap.export.wizard</field>            <form string="SIAP Export Preview">

        <field name="arch" type="xml">                <field name="export_type" invisible="1"/>

            <form string="SIAP Export Preview">                <field name="export_date" invisible="1"/>

                <field name="export_type" invisible="1"/>                <field name="format_type" invisible="1"/>

                <field name="export_date" invisible="1"/>                <field name="company_id" invisible="1"/>

                <field name="format_type" invisible="1"/>                

                <field name="company_id" invisible="1"/>                <div class="alert alert-success" role="alert">

                                    <h4>Preview: Top 20 <field name="export_type" readonly="1"/> as of <field name="export_date" readonly="1"/></h4>

                <div class="alert alert-success" role="alert">                    <p>Company: <field name="company_id" readonly="1"/></p>

                    <h4>Preview: Top 20 <field name="export_type" readonly="1"/> as of <field name="export_date" readonly="1"/></h4>                    <p>Format: <field name="format_type" readonly="1"/></p>

                    <p>Company: <field name="company_id" readonly="1"/></p>                </div>

                    <p>Format: <field name="format_type" readonly="1"/></p>                

                </div>                <field name="partner_preview_ids" readonly="1">

                                    <tree decoration-info="sequence &lt;= 5" decoration-muted="sequence &gt; 15" 

                <field name="partner_preview_ids" readonly="1">                          editable="false" create="false" delete="false">

                    <tree decoration-info="sequence &lt;= 5" decoration-muted="sequence &gt; 15"                         <field name="sequence" string="#" width="50px"/>

                          editable="false" create="false" delete="false">                        <field name="partner_id" string="Partner"/>

                        <field name="sequence" string="#" width="50px"/>                        <field name="vat" string="Tax ID"/>

                        <field name="partner_id" string="Partner"/>                        <field name="partner_country_id" string="Country" optional="hide"/>

                        <field name="vat" string="Tax ID"/>                        <field name="amount" sum="Total Amount" widget="monetary"/>

                        <field name="partner_country_id" string="Country" optional="hide"/>                        <field name="currency_id" invisible="1"/>

                        <field name="amount" sum="Total Amount" widget="monetary"/>                    </tree>

                        <field name="currency_id" invisible="1"/>                </field>

                    </tree>                

                </field>                <footer>

                                    <button string="Export to File" type="object" name="action_export" 

                <footer>                            class="btn-primary" data-hotkey="e"/>

                    <button string="Export to File" type="object" name="action_export"                     <button string="Back to Settings" type="object" name="action_back_to_wizard" 

                            class="btn-primary" data-hotkey="e"/>                            class="btn-secondary" data-hotkey="b"/>

                    <button string="Back to Settings" type="object" name="action_back_to_wizard"                     <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="x"/>

                            class="btn-secondary" data-hotkey="b"/>                </footer>

                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="x"/>            </form>

                </footer>        </field>

            </form>    </record>

        </field>

    </record>    <record id="action_siap_export_wizard" model="ir.actions.act_window">

        <field name="name">SIAP Export</field>

    <!-- Action for the wizard -->        <field name="res_model">siap.export.wizard</field>

    <record id="action_siap_export_wizard" model="ir.actions.act_window">        <field name="view_mode">form</field>

        <field name="name">SIAP Export</field>        <field name="target">new</field>

        <field name="res_model">siap.export.wizard</field>        <field name="help" type="html">

        <field name="view_mode">form</field>            <p class="o_view_nocontent_smiling_face">

        <field name="target">new</field>                Generate SIAP Export for Government Compliance

        <field name="help" type="html">            </p>

            <p class="o_view_nocontent_smiling_face">            <p>

                Generate SIAP Export for Government Compliance                Use this tool to export the top 20 customers or suppliers 

            </p>                in SIAP format for government reporting requirements.

            <p>            </p>

                Use this tool to export the top 20 customers or suppliers         </field>

                in SIAP format for government reporting requirements.    </record>

            </p>

        </field></odoo>

    </record>                    </tree>

                </field>

    <!-- Menu Structure -->                <footer>

    <menuitem                     <button string="Export to TXT" type="object" name="action_export" class="btn-primary"/>

        id="menu_siap_export"                     <button string="Back" type="object" name="action_back_to_wizard" class="btn-secondary"/>

        name="SIAP Export"                     <button string="Cancel" class="btn-secondary" special="cancel"/>

        action="action_siap_export_wizard"                 </footer>

        parent="account.menu_finance_entries"             </form>

        sequence="10"        </field>

        groups="account.group_account_user"/>    </record>



</odoo>    <record id="action_siap_export_wizard" model="ir.actions.act_window">
        <field name="name">Export to SIAP</field>
        <field name="res_model">siap.export.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_siap_export_wizard"/>
        <field name="target">new</field>
    </record>

    <menuitem id="menu_siap_export" 
              name="SIAP Export" 
              action="action_siap_export_wizard" 
              parent="account.menu_finance_entries" 
              sequence="10"/>
</odoo>
